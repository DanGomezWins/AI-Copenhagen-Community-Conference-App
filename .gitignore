# Secrets — never commit
.env
.env.*
!.env.example
*.local

# Scratch
.firecrawl/
.obsidian/

# Node
node_modules/
.next/
dist/
npm-debug.log*

# Local paste-helper for Railway variables — contains secrets
railway-vars.txt

# OCR agent test artefacts
.scan-test/

# Extracted intermediate — regenerate with npm run parse:speakers
Assets/speakers-extracted.txt

# TypeScript incremental build cache
tsconfig.tsbuildinfo

# Push token for the Open Space agenda endpoint
OPEN-SESSIONS-TOKEN.txt

# Attendee exports — names, employers and email addresses of ~120 people, and
# this repository is public. They are imported straight into Supabase and are
# never needed by the build, so they must not follow the code anywhere.
Assets/attendees-*.xlsx
Assets/attendees-*.csv
Assets/*attendee*

